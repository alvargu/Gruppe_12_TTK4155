#ifndef CAN_COM_H
#define CAN_COM_H

#include <stdint.h>


//does all initialisation of SPI and MCP can controller
//(for now inits to loopback mode only)
void can_init(void);

//can send

//can receive
#endif
